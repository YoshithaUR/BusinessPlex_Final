const questionTemplate = (email) => {
    return `
    <h1>Hello ${email}</h1>
    <p>We have received your message and will get back to you soon.</p>
    <p>Thank you for contacting us.</p>
  `;
};

export default questionTemplate;